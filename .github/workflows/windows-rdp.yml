name: Windows RDP

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: Enable RDP
      run: |
        Write-Host "Enabling RDP..." -ForegroundColor Green
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 1

    - name: Create User
      run: |
        Write-Host "Creating user..." -ForegroundColor Green
        $Password = ConvertTo-SecureString "P@ssw0rd!" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $Password
        Add-LocalGroupMember -Group "Remote Desktop Users" -Member "runneradmin"

    - name: Setup Tailscale
      run: |
        Write-Host "Installing Tailscale..." -ForegroundColor Green
        Invoke-WebRequest -Uri "https://tailscale.com/install/windows/tsinstall.exe" -OutFile "tsinstall.exe"
        Start-Process -Wait -FilePath ".\tsinstall.exe" -ArgumentList "/S"
        Write-Host "Tailscale installed!" -ForegroundColor Green

    - name: Get IP Info
      run: |
        Write-Host "Getting IP info..." -ForegroundColor Yellow
        ipconfig
        Write-Host "`n========================================" -ForegroundColor Cyan
        Write-Host "RDP CONNECTION INFO:" -ForegroundColor Green
        Write-Host "Username: runneradmin" -ForegroundColor Green
        Write-Host "Password: P@ssw0rd!" -ForegroundColor Green
        Write-Host "========================================" -ForegroundColor Cyan

    - name: Keep Alive
      run: |
        Write-Host "Keeping alive..." -ForegroundColor Green
        while ($true) { Start-Sleep 60 }
